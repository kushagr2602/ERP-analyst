# Copy to .streamlit/secrets.toml locally, or paste into the Streamlit Cloud
# "Secrets" box. Never commit the real file -- .gitignore already excludes it.
#
# Optional: providing a key lets visitors try the app without one. It is your
# key, so app/utils/demo_limits.py caps usage per session and per day.
OPENAI_API_KEY = "sk-..."
